# HealRAG-System
A RAG pipeline that audits its own answers (retrieval, hallucination, quality checks) and automatically repairs and re-checks them before responding — built with LangGraph, Groq, and FAISS, with a live Streamlit dashboard.
